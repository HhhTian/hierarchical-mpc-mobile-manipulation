from setuptools import setup, find_packages

setup(
    name="htmpc_min",        # project name
    version="0.1",                  # version
    packages=find_packages(),       # auto find packdge（controller,  simulation）
)
